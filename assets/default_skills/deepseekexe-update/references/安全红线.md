# 安全红线

## 禁止

1. 禁止在 App 中加回上传、推送或保存到远程功能。
2. 禁止把会话记录、`.credentials.yaml`、`.env` 或 API Key 加入仓库或压缩包。
3. 禁止在源码、文档、脚本中硬编码密钥。
4. 禁止在日志或提示中回显凭据。

## 必须

1. 发布前检查 `git status` 不含敏感文件。
2. 涉及凭据的排查一律使用脱敏输出。
3. 打包前检查压缩包内无意外文件。
4. 仓库保持私有。
