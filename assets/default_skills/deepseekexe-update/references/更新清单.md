# 更新清单

发布 DeepSeekExe 新版本前, 逐项确认:

## 源码检查

- [ ] `DeepSeekExe.py` 修改完成且语法检查通过(`python -m py_compile`)
- [ ] 未引入上传 / 推送功能(安全红线)
- [ ] 未硬编码任何密钥

## 构建

- [ ] exe 编译成功(`scripts\build_exe.ps1`)
- [ ] 便携版组装成功, runtime 完整(`scripts\build_portable.ps1`)
- [ ] zip 生成成功(`scripts\build_zip.py`)

## 验证

- [ ] 启动测试通过(窗口 + 端口 + 优雅退出)
- [ ] 引擎来源为 `runtime\`(便携版)
- [ ] zip 哈希一致 / UTF-8 文件名正确

## 分发

- [ ] 明确告知目标机: 替换 `DeepSeekExe.exe` 即可(`runtime\` 不动)
- [ ] 若引擎升级: 提示需要整体替换 `runtime\`
